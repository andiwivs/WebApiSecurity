
Go to http://localhost:50397/api/identity and use any credentials where username == password.

Note: Basic auth is not recommended as a security option, just doing this to show how it can be achieved.